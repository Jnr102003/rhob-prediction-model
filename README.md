# rhob-prediction-model
This project uses machine learning to predict bulk density (RHOB) from well log data. It includes preprocessing, exploratory analysis, model training, and evaluation using MAE, RMSE, and R². The model captures general trends but shows moderate performance (R² ≈ 0.446), indicating limited explanatory power.
